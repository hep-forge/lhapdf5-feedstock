#!/usr/bin/env bash

# preserve the user's existing setting
if [ ! -z "${LHAPATH+x}" ]; then
	export CONDA_BACKUP_LHAPATH="${LHAPATH}"
fi
if [ -z "${LHAPATH+x}" ]; then
        export LHAPATH="${CONDA_PREFIX}/share/LHAPDF5"
fi
